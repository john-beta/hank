{
  "name": "ProposeStructure",
  "description": "Proposes a new workspace structure based on the exploration results.\n\nUse this tool only after exploration is complete, to propose a new structure — not to describe the current workspace.\n\nUsage:\n\n- Use `proposed_workspace_entries` to structure and deliver your proposal.\n- Since proposal approvals are only valid via this tool, if the user approves a proposal outside of it, you MUST regenerate the last proposal and resubmit it via the tool. Include an explanatory text asking to confirm it officially.\n\n# Proposal Protocol\n\n## `current_path` and `proposed_path` entries\n\n- Use `current_path` to describe the 'before' state and `proposed_path` to describe the 'after' state.\n- Use always the same `current_path` for the same file for every proposal submission.\n\n## Structure rules for proposal paths\n\n- Every entry MUST have an unique path.\n- Include one entry for each file in the proposal.\n- If the curation criteria would produce duplicates, explain the conflict to the user with alternatives. Do not propose until the duplication conflict is resolved.\n- Every entry must be a file path; folders exist only as segments within a file's path, never as standalone entries. Do not propose an empty folder — a folder appears in the structure only when it contains at least one file. If the curation criteria would require a folder with no files, do not invent a placeholder file: explain the situation to the user instead.\n\n## `proposed_workspace_entries` Rebuilding\n\n- Every proposal submission must recreate the entire workspace observed in explorations, applying the necessary reorganizations.\n- If the user requests changes, regenerate and resubmit the entire proposal — no partial diffs, with the requested updates applied.\n- If the user's criteria would require excluding files, keep their entries in the proposal with `current_path` and `proposed_path` equal.\n\n# Example\n\n```json\n{\n  \"proposed_workspace_entries\": [\n    { \"current_path\": \"README.md\", \"proposed_path\": \"README.md\" },\n    { \"current_path\": \"src/Button.vue\", \"proposed_path\": \"src/components/Button.vue\" },\n    { \"current_path\": \"Card.vue\", \"proposed_path\": \"src/components/Card.vue\" }\n  ]\n}\n```",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "proposed_workspace_entries": {
        "type": "array",
        "description": "Proposed workspace structure after exploration, organized according to the requested curation criteria.",
        "items": {
          "type": "object",
          "properties": {
            "current_path": {
              "type": "string",
              "description": "The real relative path to the file from the workspace root. Always include the complete directory hierarchy ending with the file name (e.g. \"folder1/folder2/file.txt\")."
            },
            "proposed_path": {
              "type": "string",
              "description": "The desired relative path to the file from the workspace root for this proposal. Always include the complete directory hierarchy ending with the file name (e.g. \"folder1/folder2/file.txt\")."
            }
          },
          "required": ["current_path", "proposed_path"],
          "additionalProperties": false
        }
      }
    },
    "required": ["proposed_workspace_entries"],
    "additionalProperties": false
  },
  "output_schema": null
}
